---
- hosts: all
  gather_facts: no
  become: no

  vars_files:
    - ./defaults/main.yml

  tasks:
    - name: Test GeoIP Filter
      template: src=../../templates/logstash/conf.d/29-geoip-filter.conf.j2 dest=./tmp/29-geoip-filter.conf

    - name: Test HAProxy Filter
      template: src=../../templates/logstash/conf.d/16-haproxy-filter.conf.j2 dest=./tmp/16-haproxy-filter.conf
